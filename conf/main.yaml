defaults:
  - service: polygon
  - source: pulsar
  - sink: bigquery
  - method: polygon
  - mapper: evm
  - _self_
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled 
runtime:
  start_block: 0
  end_block: 10

hydra:  
  output_subdir: null  
  run:  
    dir: .