CREATE EXTERNAL STREAM blockchain__evm__block(
        number int64,
        hash string,
        parent_hash string,
        nonce string,
        sha3_uncles string,
        logs_bloom string,
        transactions_root string,
        state_root string,
        receipts_root string,
        miner string,
        mix_hash string,
        difficulty int64,
        total_difficulty int64,
        extra_data string,
        size int64,
        gas_limit int32,
        gas_used int32,
        timestamp int64,
        transactions array(string),
        uncles array(string)
)
SETTINGS type='kafka', 
        brokers='onchain-kafka-brokers.onchain.svc.cluster.local:9092',
        topic='blockchain.evm.block',
        data_format='JSONEachRow',
        one_message_per_row=true;

CREATE EXTERNAL STREAM blockchain__evm__block(
        block_number string
)
SETTINGS type='kafka', 
        brokers='onchain-kafka-brokers.onchain.svc.cluster.local:9092',
        topic='blockchain.evm.block',
        data_format='JSONEachRow',
        one_message_per_row=true;