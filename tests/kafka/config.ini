[default]
bootstrap.servers=172.18.0.3:30000


[consumer]
group.id=app
enable.partition.eof=true
# 'auto.offset.reset=earliest' to start reading from the beginning of
# the topic if no committed offsets exist.
auto.offset.reset=earliest


